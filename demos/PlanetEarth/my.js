//
let dirRight = [0, 1];
let dirLeft = [0, -1];
let dirUp = [1, 0];
let dirDown = [-1, 0];
let dirStop = [0, 0];

let my = {
  //
  dirs: [
    //
    dirRight,
    dirStop,
    dirLeft,
    dirStop,
    dirUp,
    dirStop,
    dirDown,
    dirStop,
  ],
  dirIndex: 0,
};
